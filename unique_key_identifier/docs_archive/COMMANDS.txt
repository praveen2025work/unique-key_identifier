╔════════════════════════════════════════════════════════════════╗
║        UNIQUE KEY IDENTIFIER - COMMAND LINE REFERENCE          ║
╚════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────┐
│  🚀 NEW SYSTEM SETUP                                           │
└────────────────────────────────────────────────────────────────┘

# Quick Start (3 commands):
cd /path/to/uniquekeyidentifier/unique_key_identifier
pip3 install -r requirements.txt
python3 file_comparator.py

# Or use automated script:
./run.sh              # macOS/Linux
run.bat               # Windows


┌────────────────────────────────────────────────────────────────┐
│  ▶️  START APPLICATION                                         │
└────────────────────────────────────────────────────────────────┘

# Standard start:
python3 file_comparator.py

# With auto-reload (development):
python3 -m uvicorn file_comparator:app --host 0.0.0.0 --port 8000 --reload

# Different port:
python3 -m uvicorn file_comparator:app --host 0.0.0.0 --port 8080

# Background mode:
nohup python3 file_comparator.py > app.log 2>&1 &


┌────────────────────────────────────────────────────────────────┐
│  ⏸️  STOP APPLICATION                                          │
└────────────────────────────────────────────────────────────────┘

# From terminal:
Ctrl + C

# Force stop:
pkill -9 -f file_comparator.py                    # macOS/Linux
taskkill /F /IM python.exe /FI "WINDOWTITLE eq*"  # Windows

# Kill by port:
lsof -ti:8000 | xargs kill -9                     # macOS/Linux
netstat -ano | findstr :8000                      # Windows (find PID)
taskkill /F /PID <PID>                            # Windows (kill PID)


┌────────────────────────────────────────────────────────────────┐
│  🔍 CHECK STATUS                                               │
└────────────────────────────────────────────────────────────────┘

# Check if running:
curl http://localhost:8000
lsof -i:8000                                      # macOS/Linux
netstat -ano | findstr :8000                      # Windows

# Check processes:
ps aux | grep file_comparator                     # macOS/Linux
tasklist | findstr python                         # Windows

# View logs (if running in background):
tail -f app.log


┌────────────────────────────────────────────────────────────────┐
│  📦 DEPENDENCIES                                               │
└────────────────────────────────────────────────────────────────┘

# Install all:
pip3 install -r requirements.txt

# Update all:
pip3 install --upgrade -r requirements.txt

# Install individually:
pip3 install fastapi uvicorn pandas jinja2 python-multipart xlsxwriter

# Check installed:
pip3 list | grep -E "fastapi|uvicorn|pandas"


┌────────────────────────────────────────────────────────────────┐
│  🌐 ACCESS URLS                                                │
└────────────────────────────────────────────────────────────────┘

Main UI:        http://localhost:8000
API Docs:       http://localhost:8000/docs
Results:        http://localhost:8000/run/{run_id}
Workflow:       http://localhost:8000/workflow/{run_id}


┌────────────────────────────────────────────────────────────────┐
│  🐍 VIRTUAL ENVIRONMENT (Optional)                             │
└────────────────────────────────────────────────────────────────┘

# Create:
python3 -m venv venv

# Activate:
source venv/bin/activate           # macOS/Linux
venv\Scripts\activate              # Windows

# Install dependencies:
pip install -r requirements.txt

# Deactivate:
deactivate


┌────────────────────────────────────────────────────────────────┐
│  🔧 TROUBLESHOOTING                                            │
└────────────────────────────────────────────────────────────────┘

# Port already in use:
lsof -ti:8000 | xargs kill -9

# Module not found:
pip3 install --upgrade -r requirements.txt

# Permission denied:
pip3 install --user -r requirements.txt

# Check Python version:
python3 --version    # Should be 3.7+

# Reinstall everything:
pip3 uninstall -y -r requirements.txt
pip3 install -r requirements.txt


┌────────────────────────────────────────────────────────────────┐
│  📂 FILE LOCATIONS                                             │
└────────────────────────────────────────────────────────────────┘

Data Files:     unique_key_identifier/your_file.csv/.dat/.txt
                Supported: .csv, .dat, .txt (auto-detects delimiter)
                Delimiters: comma, tab, pipe (|), semicolon, space
Application:    unique_key_identifier/file_comparator.py
Templates:      unique_key_identifier/templates/
Database:       unique_key_identifier/file_comparison.db (auto-created)
Logs:           unique_key_identifier/app.log (if background mode)


┌────────────────────────────────────────────────────────────────┐
│  📖 DOCUMENTATION                                              │
└────────────────────────────────────────────────────────────────┘

README.md          - Full documentation
SETUP_GUIDE.md     - Detailed setup instructions
QUICKSTART.md      - Essential commands only
COMMANDS.txt       - This file


┌────────────────────────────────────────────────────────────────┐
│  💡 TIPS                                                       │
└────────────────────────────────────────────────────────────────┘

✓ Always run from unique_key_identifier/ directory
✓ Data files (.csv/.dat/.txt) must be in same directory as file_comparator.py
✓ Delimiter is auto-detected - no need to specify
✓ Use ./run.sh for easiest setup
✓ Use --reload flag during development
✓ Check app.log for background process errors
✓ Use virtual environment to avoid conflicts


╔════════════════════════════════════════════════════════════════╗
║  Ready to analyze! Open http://localhost:8000 in your browser  ║
╚════════════════════════════════════════════════════════════════╝

